import 'package:flutter/material.dart';

class AppColors {
  // CSS: --glass: rgba(255, 255, 255, 0.08);
  static const Color glass = Color.fromRGBO(255, 255, 255, 0.08);

  // CSS: --glass-border: rgba(255, 255, 255, 0.15);
  static const Color glassBorder = Color.fromRGBO(255, 255, 255, 0.15);

  // CSS: --accent: #0A84FF;
  static const Color accent = Color(0xFF0A84FF);

  // CSS: --ai-color: #AF52DE;
  static const Color aiColor = Color(0xFFAF52DE);

  // Additional colors for gradients
  static const Color purple500 = Color(0xFFA855F7);
  static const Color blue500 = Color(0xFF3B82F6);
}
